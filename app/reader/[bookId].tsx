import { RoutePlaceholder } from '@/components/navigation/route-placeholder';

export default function ReaderScreen() {
  return <RoutePlaceholder subtitle="The PDF reader and reading progress will be connected in its feature phase." title="Reader" />;
}
