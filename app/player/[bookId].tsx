import { RoutePlaceholder } from '@/components/navigation/route-placeholder';

export default function PlayerScreen() {
  return <RoutePlaceholder subtitle="The audio player and listening progress will be connected in its feature phase." title="Audio Player" />;
}
